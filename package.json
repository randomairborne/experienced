{
  "name": "valk-prettier",
  "type": "module",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "prettier": "prettier . -w --plugin=prettier-plugin-astro",
    "prettier-check": "prettier . --check --plugin=prettier-plugin-astro"
  },
  "devDependencies": {
    "prettier": "^3.1.1"
  }
}
